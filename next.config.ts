import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["api.codershubinc.tech", "via.placeholder.com" , 'github.com' , 'store-images.s-microsoft.com'],
  },
};

export default nextConfig;
